# Only .md, .java and .gradle files
# Also OCaml files .ml and _tags


# ignore everything
*

# but directories
!*/

# and then whitelist the files and extensions we need
!.gitignore
!*.java
!*.md
!build.gradle
!*.ml
!_tags
